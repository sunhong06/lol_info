export interface summonerInfoType {
  freshBlood?: boolean;
  hotStreak?: boolean;
  inactive?: boolean;
  leaguePoints: number;
  losses: number;
  rank: number;
  summonerId: string;
  summonerName: string;
  veteran?: boolean;
  wins: number;
}
